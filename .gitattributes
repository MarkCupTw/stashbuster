# 统一行尾，避免 Windows 上的 core.autocrlf=true 把 shell 脚本转成 CRLF。
# 不写这个文件的话，Linux / macOS 用户 clone 下来的 gradlew 会因为
# 行尾变成 CRLF 而报 "bad interpreter: No such file or directory"。

# 默认：仓库与工作区都用 LF
* text=auto eol=lf

# Windows 批处理必须是 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# POSIX 脚本必须是 LF（显式声明，防止被上面的规则或用户配置覆盖）
gradlew text eol=lf
*.sh text eol=lf

# 二进制文件不要做任何转换
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.nbt binary
